//Multiple Roles
Route::group(['middleware' => ['role:admin,user']], function () {
//       Routes
});

//Single role
Route::group(['middleware' => ['role:admin']], function () {
//       Routes
})


//Multiple Plan
Route::group(['middleware' => ['plan:base,lite']], function () {
//       Routes
});

//Single Plans
Route::group(['middleware' => ['plan:base']], function () {
//       Routes
})


check PLANS
@can('lite')
or
@can('base')
    <!-- This content will be visible only to users with the 'edit articles' permission -->
    <p>You have permission to edit articles.</p>
@else
    <!-- This content will be visible to users without the 'edit articles' permission -->
    <p>You do not have permission to edit articles.</p>
@endcan

Check roles


@role('admin')
    <!-- This content will be visible only to users with the 'admin' role -->
    <p>You are an admin.</p>
@else
    <!-- This content will be visible to users without the 'admin' role -->
    <p>You are not an admin.</p>
@endrole



USE stack

In page -
@push('scripts')
    <script src="example.js"></script>
@endpush

In lauout
@stack('scripts_footer')



Незабудь дамп автолоад зробити для хелперів
